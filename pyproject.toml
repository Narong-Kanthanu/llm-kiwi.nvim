[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = [
  "E501", # line length (module has large embedded HTML)
]

[tool.ruff.lint.per-file-ignores]
"scripts/vault-graph.py" = ["E741"]
